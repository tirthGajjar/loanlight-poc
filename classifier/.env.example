# Local development environment variables.
# Copy this file to `.env` and fill in the values.
# See also: .env.vercel.example and .env.trigger.example for production.
#
# When adding variables, update the schema in "/src/env.js" accordingly.

# ── Shared (Vercel + Trigger.dev) ──────────────────────────────────────────

# Prisma / Neon
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
DATABASE_URL="postgresql://postgres:password@localhost:5434/classifier"

# S3 / MinIO
# Endpoint is optional — omit for real AWS S3, set for MinIO or R2
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="loanlight-documents"

# LlamaCloud
LLAMACLOUD_API_KEY=""

# ── Vercel-only ────────────────────────────────────────────────────────────

# Better Auth (generate secret with: openssl rand -base64 32)
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# Email (Resend required in production, Mailpit in dev)
RESEND_API_KEY=""
EMAIL_FROM="LoanLight <onboarding@resend.dev>"
LOCAL_ONLY_SMTP_HOST="localhost"
LOCAL_ONLY_SMTP_PORT="1025"

# Trigger.dev (get from dashboard: Project → API Keys)
TRIGGER_SECRET_KEY=""

# ── Dev-only ───────────────────────────────────────────────────────────────

# Seed script (optional) — create an admin user with `pnpm db:seed`
SEED_ADMIN_EMAIL=""
SEED_ADMIN_PASSWORD=""
SEED_ADMIN_NAME="Admin"
